---
order: 84
title: F检验
module: 'probability-statistics'
category: 'comp-sci'
difficulty: intermediate
description: 两总体方差比的F检验、单因素方差分析（ANOVA）的原理与应用，以"比较两个班级的方差"类比贯穿。
author: fanquanpp
updated: '2026-08-02'
related:
  - 'probability-statistics/t检验'
  - 'probability-statistics/卡方检验'
  - 'probability-statistics/假设检验典型例题'
prerequisites:
  - 'probability-statistics/样本空间与事件'
---

## 0. 比较两个班级的成绩波动

两个平行班期末数学成绩的平均分一样（都是 75 分），但甲班最高 98、最低 52，乙班集中在 70-80 之间。作为年级组长，你会问：**两个班的成绩波动（方差）有显著差异吗？**

这就是 **$F$ 检验**的用武之地：**比较两个正态总体的方差是否相等**（方差齐性检验）。它也是 $t$ 检验的前置检查——做双总体 $t$ 检验前，通常先确认方差是否相等（相等用合并方差，不等用 Welch）。

$F$ 检验的思想极其简单：方差比！

$$F = \frac{S_1^2}{S_2^2} \sim F(n_1-1, n_2-1)$$

如果两个方差真相等，比值应接近 1；比值过大或过小，说明"波动有差异"。

本文用**比较驱动**的方式组织：先讲 $F$ 统计量与两方差比检验，再扩展到一个更强大的"比较工具"——**单因素方差分析（ANOVA）**（比较多个总体的均值），最后说明 $F$ 检验在 $t$ 检验中的作用与前提条件。

## 1. 两总体方差比的 F 检验

### 1.1 适用条件

两个正态总体 $N(\mu_1, \sigma_1^2)$ 和 $N(\mu_2, \sigma_2^2)$，两样本独立，检验方差是否相等（**方差齐性检验**）。

### 1.2 检验步骤

**假设**：

- 双侧：$H_0: \sigma_1^2 = \sigma_2^2$，$H_1: \sigma_1^2 \neq \sigma_2^2$
- 右侧：$H_0: \sigma_1^2 \leq \sigma_2^2$，$H_1: \sigma_1^2 > \sigma_2^2$
- 左侧：$H_0: \sigma_1^2 \geq \sigma_2^2$，$H_1: \sigma_1^2 < \sigma_2^2$

**检验统计量**（抽样分布定理七，$\sigma_1^2 = \sigma_2^2$ 时）：

$$F = \frac{S_1^2}{S_2^2} \sim F(n_1 - 1, n_2 - 1)$$

**拒绝域**：

| 检验类型 | 拒绝域 |
| --- | --- |
| 双侧 | $F > F_{\alpha/2}(n_1-1, n_2-1)$ 或 $F < F_{1-\alpha/2}(n_1-1, n_2-1)$ |
| 右侧 | $F > F_\alpha(n_1-1, n_2-1)$ |
| 左侧 | $F < F_{1-\alpha}(n_1-1, n_2-1) = \dfrac{1}{F_\alpha(n_2-1, n_1-1)}$ |

> **实用约定**：双侧检验时，习惯把**较大的样本方差放分子**，使 $F \geq 1$，只查上侧分位数 $F_{\alpha/2}$ 即可——这简化了查表。

### 1.3 完整例题

**例题**：甲乙两台机床加工同种零件，分别抽取 10 件和 8 件，测得 $s_1^2 = 0.064$，$s_2^2 = 0.030$。在 $\alpha = 0.05$ 下检验两机床加工精度是否有显著差异（方差是否相等）。

**解**：

$$H_0: \sigma_1^2 = \sigma_2^2, \quad H_1: \sigma_1^2 \neq \sigma_2^2 \quad \text{（双侧）}$$

$S_1^2 = 0.064 > S_2^2 = 0.030$，较大的放分子（分子 $n_1 - 1 = 9$，分母 $n_2 - 1 = 7$）：

$$F = \frac{0.064}{0.030} = 2.133$$

查表 $F_{0.025}(9, 7) = 4.82$。$F = 2.133 < 4.82$，**不拒绝 $H_0$**——没有显著证据表明两机床加工精度（方差）不同。

### 1.4 F 检验在 t 检验中的作用

做双总体 $t$ 检验前，标准流程是：

1. 先用 $F$ 检验判断 $\sigma_1^2$ 是否等于 $\sigma_2^2$；
2. 若相等 → 用合并方差（Student）$t$ 检验；
3. 若不等 → 用 Welch $t$ 检验。

这就是"先比波动（$F$），再比位置（$t$）"的流水线。

## 2. 单因素方差分析（ANOVA）

### 2.1 问题

$F$ 检验能比较**两个**方差；而**方差分析**把比较扩展到**多个总体**：$k$ 个正态总体的均值是否全相等？比如三种饲料、四种教学方法、五种材料的比较。

**为什么叫"方差分析"**：虽然问的是均值，方法却从"方差的分解"入手——把总波动拆成"组间波动"与"组内波动"两部分，用比值做检验。

### 2.2 模型

设 $k$ 个正态总体 $N(\mu_i, \sigma^2)$（$i = 1, 2, \cdots, k$），方差相等（方差齐性），从第 $i$ 个总体抽取 $n_i$ 个样本。

$$H_0: \mu_1 = \mu_2 = \cdots = \mu_k, \quad H_1: \mu_i \text{ 不全相等}$$

### 2.3 平方和的分解

记总均值 $\bar{X} = \dfrac{1}{n}\sum_{i,j} X_{ij}$，第 $i$ 组均值 $\bar{X}_i$，$n = \sum n_i$。

**总平方和**：

$$S_T = \sum_{i=1}^k \sum_{j=1}^{n_i} (X_{ij} - \bar{X})^2$$

**组间平方和**（各组均值与总均值的偏离，反映"处理效应"）：

$$S_A = \sum_{i=1}^k n_i(\bar{X}_i - \bar{X})^2$$

**组内平方和**（各组内部波动，反映"随机误差"）：

$$S_E = S_T - S_A = \sum_{i=1}^k \sum_{j=1}^{n_i} (X_{ij} - \bar{X}_i)^2$$

核心恒等式：$S_T = S_A + S_E$——总波动 = 处理效应 + 随机误差。

### 2.4 方差分析表

| 来源 | 平方和 | 自由度 | 均方 | F 值 |
| --- | --- | --- | --- | --- |
| 组间（因素） | $S_A$ | $k-1$ | $\bar{S}_A = \dfrac{S_A}{k-1}$ | $F = \dfrac{\bar{S}_A}{\bar{S}_E}$ |
| 组内（误差） | $S_E$ | $n-k$ | $\bar{S}_E = \dfrac{S_E}{n-k}$ | |
| 总和 | $S_T$ | $n-1$ | | |

$H_0$ 下 $F = \dfrac{\bar{S}_A}{\bar{S}_E} \sim F(k-1, n-k)$。

**拒绝域**：$F > F_\alpha(k-1, n-k)$。

**直觉**：若 $H_0$ 成立，组间波动与组内波动都只是随机误差，比值应接近 1；组间波动明显大于组内波动（$F$ 很大），说明"分组确实造成了差异"。

### 2.5 完整例题

**例题**：三种饲料对猪增重（kg）的试验数据如下：

饲料A：51, 40, 43, 48
饲料B：23, 32, 28, 35
饲料C：23, 25, 26, 18

在 $\alpha = 0.05$ 下检验三种饲料效果是否有显著差异。

**解**：

各 $n_i = 4$，$k = 3$，$n = 12$。

组均值与总均值：

$$\bar{X}_A = \frac{51+40+43+48}{4} = 45.5, \quad \bar{X}_B = \frac{23+32+28+35}{4} = 29.5, \quad \bar{X}_C = \frac{23+25+26+18}{4} = 23$$

$$\bar{X} = \frac{45.5 + 29.5 + 23}{3} = 32.67$$

组间平方和：

$$S_A = 4(45.5 - 32.67)^2 + 4(29.5 - 32.67)^2 + 4(23 - 32.67)^2 = 4 \times 164.69 + 4 \times 10.05 + 4 \times 93.51 = 658.76 + 40.20 + 374.04 = 1073.0$$

组内平方和：

$$S_E = \sum (X_{ij} - \bar{X}_i)^2 = [(51-45.5)^2 + (40-45.5)^2 + (43-45.5)^2 + (48-45.5)^2] + [(23-29.5)^2 + (32-29.5)^2 + (28-29.5)^2 + (35-29.5)^2] + [(23-23)^2 + (25-23)^2 + (26-23)^2 + (18-23)^2]$$

$$= (30.25 + 30.25 + 6.25 + 6.25) + (42.25 + 6.25 + 2.25 + 30.25) + (0 + 4 + 9 + 25) = 73 + 81 + 38 = 192$$

方差分析表：

| 来源 | 平方和 | 自由度 | 均方 | F 值 |
| --- | --- | --- | --- | --- |
| 组间 | 1073.0 | 2 | 536.5 | $\dfrac{536.5}{21.33} = 25.15$ |
| 组内 | 192 | 9 | 21.33 | |
| 总和 | 1265 | 11 | | |

查表 $F_{0.05}(2, 9) = 4.26$。$F = 25.15 > 4.26$，**拒绝 $H_0$**——三种饲料效果有显著差异。

**补充**：拒绝后自然想知道"谁和谁不同"，这需要事后多重比较（如 Tukey HSD），超出本文范围，仅作提示。

## 3. F 检验与方差分析的前提条件

1. **正态性**：各总体服从正态分布（或近似）；
2. **方差齐性**：各总体方差相等（可用 $F$ 检验或 Levene 检验验证）；
3. **独立性**：各样本相互独立。

当方差齐性不满足时：

- 两组比较 → Welch $t$ 检验；
- 多组比较 → Welch 方差分析或非参数方法（如 Kruskal-Wallis 检验）。

### 3.4 F 分布与其他分布的关系

回顾《三大分布》的图谱，$F$ 分布并非孤立存在：

- 若 $T \sim t(n)$，则 $T^2 \sim F(1, n)$——均值检验的 $t$ 统计量平方后就是 $F$ 统计量；
- $\chi^2(n)/n$ 是 $F(n, \infty)$ 的极限情形（分母自由度趋于无穷）；
- $F$ 分布与 $\chi^2$ 分布同源于"平方和之比"，只是 $\chi^2$ 看"一个平方和"，$F$ 看"两个平方和的比值"。

**应用提示**：当组数 $k = 2$ 时，单因素方差分析的 $F$ 检验与双总体等方差 $t$ 检验**完全等价**，且统计量满足 $F = T^2$（对应自由度 $F(1, n_1+n_2-2)$ 与 $t(n_1+n_2-2)$）。这意味着"两个班比较"（$t$ 检验）与"多个班比较"（ANOVA）在数学上是统一的——ANOVA 是 $t$ 检验对多组的自然推广。

## 4. 常见错误与对策

| 错误类型 | 错误示例 | 原因分析 | 纠正方法 |
| --- | --- | --- | --- |
| 自由度颠倒 | $F$ 检验把 $F(n_1-1, n_2-1)$ 写成 $F(n_2-1, n_1-1)$ | 分子分母弄反 | 第一自由度 = 分子样本量减 1，第二自由度 = 分母样本量减 1 |
| 双侧只查上侧 | 双侧检验不把大方差放分子就只查上侧 | 查表不便 | 约定：大方差放分子，$F \geq 1$，只查 $F_{\alpha/2}(n_1-1, n_2-1)$ |
| 方差分析混淆平方和 | $S_A$ 与 $S_E$ 算反 | 不知分解定义 | $S_A$ = 组间（组均值对总均值），$S_E$ = 组内（组内对组均值），$S_T = S_A + S_E$ 用于校验 |
| 自由度配错 | ANOVA 组内自由度写成 $n-1$ | 不知分解 | 组间 $k-1$，组内 $n-k$，总和 $n-1$；三者相加校验 |
| 忘记方差齐性前提 | 方差悬殊仍做标准 ANOVA | 忽略前提 | 先做方差齐性检验；不满足改用 Welch 或非参数法 |
| 显著即两两都不同 | ANOVA 拒绝后直接断言"各组两两都不同" | 混淆整体与两两 | 拒绝只说明"不全相等"，需事后多重比较找差异组 |

## 5. 实战练习

**练习 1（两方差比）**：两种型号灯泡的寿命服从正态分布，各抽取 10 只和 8 只，$s_1^2 = 2600$，$s_2^2 = 1100$。在 $\alpha = 0.05$ 下检验两种灯泡寿命方差是否相等。

**提示**：大方差放分子，$F = \dfrac{2600}{1100} = 2.364$，$F_{0.025}(9, 7) = 4.82$。

**参考答案要点**：$2.364 < 4.82$，不拒绝 $H_0$，方差无显著差异。

**练习 2（方差齐性判断）**：$n_1 = 15$，$s_1^2 = 8.4$；$n_2 = 12$，$s_2^2 = 3.6$，$\alpha = 0.05$ 双侧检验方差是否相等。

**提示**：$F = 8.4/3.6 = 2.333$，查 $F_{0.025}(14, 11)$。

**参考答案要点**：$F_{0.025}(14, 11) \approx 3.09$（查表近似值），$2.333 < 3.09$，不拒绝 $H_0$，方差齐性成立，可继续用合并方差 $t$ 检验。

**练习 3（ANOVA 自由度）**：4 组数据，每组样本量分别为 5, 6, 5, 4。写出 ANOVA 中组间、组内、总和的自由度。

**提示**：$k = 4$，$n = 20$。

**参考答案要点**：组间 $k-1 = 3$，组内 $n-k = 16$，总和 $n-1 = 19$。

**练习 4（ANOVA 计算）**：三种教学方法的学生成绩如下：

方法A：73, 66, 89, 82, 43, 80, 63
方法B：88, 78, 91, 76, 85, 84
方法C：68, 79, 71, 71, 87

在 $\alpha = 0.05$ 下检验三种教学方法效果是否有显著差异。

**提示**：$\bar{X}_A = 70.86$，$\bar{X}_B = 83.67$，$\bar{X}_C = 75.2$，$\bar{X} = 76.17$；$F_{0.05}(2, 15) = 3.68$。

**参考答案要点**：$S_A = 7(70.86-76.17)^2 + 6(83.67-76.17)^2 + 5(75.2-76.17)^2 = 197.5 + 337.5 + 4.7 = 539.7$；$S_E = 1516.9 + 154.8 + 268.8 = 1940.5$；$F = \dfrac{539.7/2}{1940.5/15} = \dfrac{269.85}{129.37} = 2.086 < 3.68$，不拒绝 $H_0$，三种教学方法无显著差异。

**练习 5（综合：先 F 后 t）**：甲工艺 $n_1 = 9$，$s_1^2 = 1.8$；乙工艺 $n_2 = 11$，$s_2^2 = 0.9$。（a）$\alpha = 0.10$ 下检验方差是否相等；（b）据此说明应选哪种 $t$ 检验。

**提示**：$F = 2.0$，$F_{0.05}(8, 10) = 3.07$。

**参考答案要点**：（a）$2.0 < 3.07$，不拒绝 $H_0$，可认为方差齐性成立；（b）选用合并方差（Student）$t$ 检验；若 (a) 拒绝了，则改用 Welch $t$ 检验。

## 6. 一句话记忆

**F 检验是"波动比较器"：两个方差比 $F = \frac{S_1^2}{S_2^2} \sim F(n_1-1, n_2-1)$，大方差放分子查上侧；方差分析把 $S_T = S_A + S_E$ 分解后算 $F = \frac{\bar{S}_A}{\bar{S}_E} \sim F(k-1, n-k)$，先比波动（$F$）再比均值（$t$）。**

## 参考文献

- 盛骤、谢式千、潘承毅，《概率论与数理统计》（浙大版第四版），高等教育出版社：第八章"方差比的检验"与第十章"方差分析"。
- 浙江大学概率论与数理统计 MOOC 第 64-66 讲（$F$ 检验与方差分析）：https://www.icourse163.org/course/ZJU-1001743002
- Khan Academy 统计与概率（ANOVA 模块）：https://zh.khanacademy.org/math/statistics-probability
- OpenIntro Statistics：https://www.openintro.org/book/os/
- Seeing Theory（交互式可视化统计教材）：https://seeing-theory.brown.edu/

## 延伸阅读

- 假设检验思想框架（原假设、两类错误），见《假设检验基本概念》。
- 双总体 $t$ 检验（等方差/不等方差），见《t检验》。
- 单总体方差检验与拟合优度检验，见《卡方检验》。
- 各检验的综合选择与应用，见《假设检验典型例题》。
