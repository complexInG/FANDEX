---
order: 150
title: SVG
module: 'html5'
category: 前端技术
difficulty: intermediate
description: 基本形状、viewBox、路径、文本、滤镜
author: fanquanpp
updated: '2026-08-02'
related:
  - 'html5/013-ImageResponsiveImage'
  - 'html5/014-AudioVideo'
  - 'html5/016-EmbeddedContent'
  - 'html5/017-ProgressMeter'
  - 'svg/001-SVGOverviewEnvSetup'
  - 'svg/003-SVGCoordinateSystemViewBox'
prerequisites:
  - 'html5/001-HTML5OverviewCoreFeature'
---

## 0. 直觉：SVG 是“用代码画的图”

普通图片（JPG/PNG）像照片：放大到一定程度就模糊，因为它是像素点阵。SVG 像“数学描述的画”：记录的是圆形、线条、坐标这些信息，放大多少倍都清晰，而且可以用代码生成、用 CSS 改颜色。

网页上的 Logo、图标、图表、插画，很多都是 SVG。它和 `<img>` 的区别：`img` 是“贴一张照片”，SVG 是“在页面上直接画”。

## 1. SVG 基础

```html
<svg width="200" height="200" xmlns="http://www.w3.org/2000/svg">
  <circle cx="100" cy="100" r="80" fill="blue" />
</svg>
```

**讲解：**

- `<svg>` 是画布根元素，`width`/`height` 决定显示尺寸；
- `<circle>` 的 `cx`/`cy` 是圆心坐标，`r` 是半径，`fill` 是填充色；
- SVG 里的每个图形都是一个“元素”，可以被 CSS 和 JavaScript 单独操作。

## 2. viewBox 详解

`viewBox="min-x min-y width height"` 定义 SVG 的内部坐标系统：

```html
<svg width="400" height="300" viewBox="0 0 200 150">
  <rect x="0" y="0" width="100" height="75" fill="blue" />
</svg>
```

**讲解：**

- `viewBox="0 0 200 150"` 定义内部坐标系：内容按 200x150 设计，再缩放到 400x300 的显示区域；
- 缩放是等比无损的，这是 SVG 放不大糊的核心原因；
- `preserveAspectRatio` 控制缩放对齐方式：`meet` 完整显示、`slice` 裁剪填充、`none` 拉伸。

| preserveAspectRatio 值 | 说明                   |
| ---------------------- | ---------------------- |
| `xMidYMid meet`        | 居中，完整显示（默认） |
| `xMidYMid slice`       | 居中，裁剪填充         |
| `none`                 | 不保持比例             |

## 3. 基本形状

```html
<svg width="400" height="300">
  <rect x="10" y="10" width="100" height="60" rx="10" fill="blue" />
  <circle cx="200" cy="80" r="50" fill="red" />
  <ellipse cx="320" cy="80" rx="60" ry="30" fill="green" />
  <line x1="10" y1="150" x2="390" y2="150" stroke="black" />
  <polyline points="10,180 50,160 90,200" fill="none" stroke="purple" />
  <polygon points="200,180 240,220 160,220" fill="orange" />
</svg>
```

**讲解：**

- `rect` 矩形（`rx` 圆角）、`circle` 圆、`ellipse` 椭圆、`line` 直线；
- `polyline` 折线（不闭合），`polygon` 多边形（自动闭合）；
- 描边统一用 `stroke` 与 `stroke-width`，填充用 `fill`。

## 4. 路径 path

| 命令 | 说明       | 示例                  |
| ---- | ---------- | --------------------- |
| `M`  | 移动到     | `M 10 10`             |
| `L`  | 直线到     | `L 100 100`           |
| `C`  | 三次贝塞尔 | `C 20,20 40,20 50,10` |
| `Q`  | 二次贝塞尔 | `Q 50,0 100,50`       |
| `A`  | 弧线       | `A 25,25 0 0,1 50,25` |
| `Z`  | 闭合路径   | `Z`                   |

> 小写字母为相对坐标，大写字母为绝对坐标。

**讲解：** `d` 属性是路径的“绘图指令串”：`M` 落笔、`L` 画直线、`C`/`Q` 画曲线、`A` 画弧线、`Z` 收笔闭合。写复杂图标时通常由设计工具导出，手写只需要看懂基本命令。

## 5. 文本

```html
<svg>
  <text x="20" y="50" font-size="24" fill="black">Hello SVG</text>
  <defs><path id="curve" d="M 50 150 Q 200 50, 350 150" /></defs>
  <text font-size="20"><textPath href="#curve">沿曲线排列的文字</textPath></text>
</svg>
```

**讲解：**

- `<text>` 在 SVG 中绘制文字，`x`/`y` 是基线坐标；
- `<defs>` 存放可复用的定义（如路径、渐变、滤镜），本身不显示；
- `<textPath>` 让文字沿 `<path>` 排列，适合做环形或波浪文字效果。

## 6. 滤镜与渐变

```html
<svg>
  <defs>
    <filter id="blur"><feGaussianBlur stdDeviation="5" /></filter>
    <filter id="shadow"><feDropShadow dx="4" dy="4" stdDeviation="3" /></filter>
    <linearGradient id="lg" x1="0%" x2="100%">
      <stop offset="0%" stop-color="red" />
      <stop offset="100%" stop-color="blue" />
    </linearGradient>
  </defs>
  <rect x="50" y="50" width="100" height="80" fill="url(#lg)" filter="url(#shadow)" />
</svg>
```

**讲解：**

- `<filter>` 定义滤镜，`feGaussianBlur` 高斯模糊、`feDropShadow` 投影；
- `<linearGradient>` 定义线性渐变，`<stop>` 是颜色断点；
- 使用时通过 `fill="url(#id)"` 和 `filter="url(#id)"` 引用定义。

## 7. 进阶知识点

### 7.1 g 分组与 use 复用

```html
<!-- g 分组：统一施加变换与样式 -->
<g transform="translate(50, 50)" fill="red" stroke="black">
  <rect width="50" height="50" />
  <circle cx="80" cy="25" r="25" />
</g>

<!-- defs + symbol 定义一次，use 多次引用 -->
<defs>
  <symbol id="icon" viewBox="0 0 24 24">
    <circle cx="12" cy="12" r="10" />
  </symbol>
</defs>
<use href="#icon" x="0" y="0" width="50" height="50" />
<use href="#icon" x="60" y="0" width="50" height="50" fill="red" />
```

**讲解：**

- `<g>` 是分组容器，`transform`/`fill` 等属性会传递给组内所有元素；
- `<symbol>` 定义可复用图标，配合 `<use>` 可在任意位置多次引用；
- 这是 SVG 图标系统的核心：一个定义、多处使用、按需改色。

### 7.2 SVG 动画

```html
<rect x="0" y="0" width="50" height="50" fill="red">
  <animate attributeName="x" from="0" to="200" dur="2s" repeatCount="indefinite" />
</rect>

<circle cx="50" cy="50" r="20" fill="blue">
  <animate attributeName="r" values="20;40;20" dur="2s" repeatCount="indefinite" />
</circle>
```

**讲解：**

- `<animate>` 是 SVG 原生（SMIL）动画：`attributeName` 指定要动画的属性，`from`/`to` 或 `values` 定义变化；
- `dur` 是时长，`repeatCount="indefinite"` 无限循环；
- 也可以用 CSS `@keyframes` 驱动 SVG 属性，两者按项目习惯选择。

### 7.3 在 HTML 中使用 SVG

```html
<img src="icon.svg" alt="图标" />

<svg>...</svg>

<object data="icon.svg" type="image/svg+xml"></object>
```

**讲解：**

- `<img src="*.svg">` 最简单，但无法从外部改颜色；
- 内联 `<svg>` 直接写在 HTML 中，可被 CSS 控制颜色、尺寸与动画；
- `<object>` 适合需要脚本隔离的场景，现代项目用得少。

## 8. 动手试试

### 入门版（必做）

1. 用 `<circle>` 和 `<rect>` 画一个简单的“太阳和房子”；
2. 给房子加一个 `viewBox="0 0 100 100"`，观察不同 `preserveAspectRatio` 的效果；
3. 用 `<text>` 在图形下方加一行说明文字。

### 进阶版（选做）

1. 用 `<symbol>` + `<use>` 做一个 3 个相同图标的工具栏，第二个改成红色；
2. 用 `<animate>` 让圆点沿水平方向来回移动；
3. 用 CSS 给内联 SVG 加悬停变色效果。

## 9. 核心知识点

> 一句话记住 SVG：矢量图形用代码画，放大不糊；`svg` 是画布，`g` 分组，`use` 复用；`viewBox` 定坐标，`path` 画任意形状。

- SVG 是矢量格式：无损缩放、可被 CSS/JS 操作，适合图标与图表；
- `viewBox` 定义内部坐标系，`preserveAspectRatio` 控制对齐；
- 基本形状：`rect`/`circle`/`ellipse`/`line`/`polyline`/`polygon`；
- `path` 用 `M`/`L`/`C`/`A`/`Z` 等命令绘制任意路径；
- `defs` + `symbol` + `use` 实现图标复用，`g` 实现分组；
- 动画可用 SMIL `<animate>` 或 CSS。

## 10. 注意事项与改进建议

| 问题点 | 说明 | 改进方案 |
| --- | --- | --- |
| 图片用位图硬放 | 放大模糊、体积大 | 图标/Logo 改用 SVG |
| 忘记 `viewBox` | 坐标混乱、缩放变形 | 统一用 `viewBox` 定义坐标系 |
| 手写复杂 `path` | 易错且难以维护 | 用设计工具导出，或复用图标库 |
| `<img>` 引用 SVG | 无法用 CSS 改色 | 需要改色时内联 SVG |
| 滤镜滥用 | 渲染性能下降 | 滤镜只用于小范围图形 |
| 缺少 `xmlns` | 部分环境无法识别 | 根 `<svg>` 保留 `xmlns` 声明 |

## 11. 扩展学习

- 坐标系：`svg/003-SVGCoordinateSystemViewBox` 深入 viewBox 与坐标变换；
- 动画：`css/017-CSSAnimationTransition` 对比 CSS 动画与 SMIL；
- 图标系统：`html5/006-HTML5MultimediaCanvasDrawing` 中 SVG 图标示例；
- 绘图选型：`html5/006-HTML5MultimediaCanvasDrawing` 中 Canvas 与 SVG 对比。
