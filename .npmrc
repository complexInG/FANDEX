# =============================================================================
# FANDEX 根 .npmrc — pnpm monorepo 认证与注册表配置
# =============================================================================
# pnpm 11 变更说明：
# - .npmrc 仅处理认证与注册表相关设置
# - pnpm 特有设置（nodeLinker/strictDepBuilds/allowBuilds 等）已迁移至 pnpm-workspace.yaml
# - package.json 的 pnpm 字段不再被读取
# =============================================================================
